## This is my portfolio website built on Next.js/React Framework

### Considerations

- `cloudflare` git branch is used for Cloudflare continious deployment process.

### To build the app

- `npm run build`

### To start the app locally

- `npm run dev`